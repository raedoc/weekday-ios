platform :ios, '7.0'

target :WeekdayTests do
  pod 'Specta',      '~> 0.2'
  pod 'Expecta',     '~> 0.2'   # expecta matchers
  pod 'OCMock'
end

target 'Acceptance Tests', :exclusive => true do
  pod 'Specta',      '~> 0.2'
  pod 'KIF', '~> 3.0'
end
